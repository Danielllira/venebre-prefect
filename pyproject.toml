[project]
name = "venebre-prefect"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "prefect>=3.6.27",
]

[dependency-groups]
dev = [
    "flake8>=7.3.0",
    "pytest>=9.0.3",
    "ruff>=0.15.11",
]
