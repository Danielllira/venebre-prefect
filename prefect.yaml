name: venebre-prefect

build: null
push: null

pull:
  - prefect.deployments.steps.set_working_directory:
      directory: /github/workspace

deployments:
  - name: hello-dev
    entrypoint: pipelines/extract/test_pipeline/hello.py:hello_flow
    work_pool:
      name: venebre-pool
    parameters:
      env: dev